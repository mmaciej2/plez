def fontsize(size):
    return {
        "font.size": f"{size}",
        "axes.titlesize": f"{size}",
        "axes.labelsize": f"{size}",
        "xtick.labelsize": f"{size}",
        "ytick.labelsize": f"{size}",
        "legend.fontsize": f"{size}",
        "figure.titlesize": f"{size}",
        "figure.labelsize": f"{size}",
    }
